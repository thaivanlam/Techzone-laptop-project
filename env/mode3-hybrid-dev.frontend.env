# =====================================================================
# Mode 3 — Hybrid dev: infra in Docker, business services run from the IDE
# Target: copy to frontend/.env
#   cp env/mode3-hybrid-dev.frontend.env frontend/.env
# Pair with env/mode3-hybrid-dev.backend.env -> backend/.env
# See docs/operations/running-locally.md#mode-3--hybrid-dev
# No secrets included — fill in the blanks locally, never commit the result.
# =====================================================================

# The browser calls the gateway directly, so CORS is in play:
# FRONTEND_URL in backend/.env must match this origin.
VITE_BACK_END_URL=http://localhost:8080
VITE_FRONTEND_URL=http://localhost:5173

VITE_STRIPE_PUBLISHABLE_KEY=
